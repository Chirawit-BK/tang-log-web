# TangLog Web Environment Variables

# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api

# =============================================================================
# OAuth Configuration
# =============================================================================

# Google OAuth (Google Identity Services)
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application)
# 3. Add authorized JavaScript origins: http://localhost:5173 (dev), your production domain
# 4. Add authorized redirect URIs: http://localhost:5173/auth/google/callback
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Apple Sign-In (Optional - feature-flagged)
# 1. Go to https://developer.apple.com/account/resources/identifiers/list/serviceId
# 2. Create a Services ID
# 3. Configure Sign in with Apple:
#    - Domain: your domain (must be verified)
#    - Return URL: https://yourdomain.com/auth/apple/callback
# NOTE: Apple Sign-In requires HTTPS and domain verification
# Leave empty to disable Apple Sign-In button
VITE_APPLE_CLIENT_ID=
VITE_APPLE_REDIRECT_URI=

# Feature Flags
# Set to 'true' to enable Apple Sign-In (requires proper Apple configuration)
VITE_ENABLE_APPLE_SIGNIN=false
